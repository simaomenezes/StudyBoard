PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USER=studyboard
DB_PASSWORD=studyboard
DB_NAME=studyboard
# Set to true for local dev auto-sync (never in production; use migrations)
DB_SYNC=true
# Comma-separated list of frontend origins allowed to call the API (browser CORS)
CORS_ORIGIN=http://localhost:5173
